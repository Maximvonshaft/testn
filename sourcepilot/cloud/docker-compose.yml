services:
  sourcepilot:
    image: python:3.13-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: bash -lc "pip install -r requirements.txt && python -m app.seed && uvicorn app.main:app --host 0.0.0.0 --port 8088"
    ports:
      - "8088:8088"
